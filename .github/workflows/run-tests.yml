name: run_tests
on: push

jobs:
  build_and_test_with_signing:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Create config file
        run: |
          touch OpenProjectAPI.xcconfig
          echo OPEN_PROJECT_CONSUMER_KEY="key" >> OpenProjectAPI.xcconfig
          cat OpenProjectAPI.xcconfig
        
      - name: Install the Apple certificate and provisioning profile
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # create variables
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          # import certificate and provisioning profile from secrets
          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode --output $CERTIFICATE_PATH
          echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode --output $PP_PATH

          # create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # import certificate to keychain
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          # apply provisioning profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles
          
      - name: Build app
        run: |
         xcodebuild clean build -project "OpenProjectTimeTracker.xcodeproj" -scheme "OpenProjectTimeTracker" -destination 'platform=iOS Simulator,name=iPhone 13,OS=15.2'
         
      - name: Test app
        run: |
          xcodebuild test -project "OpenProjectTimeTracker.xcodeproj" -scheme "OpenProjectTimeTracker" -destination 'platform=iOS Simulator,name=iPhone 13,OS=15.2'

      - name: Success badge
        if: success()
        uses: schneegans/dynamic-badges-action@v1.4.0
        with:
            auth: ${{ secrets.SHIELDS_GIST_TOKEN }}
            gistID: ${{ secrets.GIST_ID }}
            filename: TimeTrackerShields.json
            label: Build
            message: success
            color: brightgreen
            
      - name: Failure badge
        if: failure()
        uses: schneegans/dynamic-badges-action@v1.4.0
        with:
            auth: ${{ secrets.SHIELDS_GIST_TOKEN }}
            gistID: ${{ secrets.GIST_ID }}
            filename: TimeTrackerShields.json
            label: Build
            message: failure
            color: red
